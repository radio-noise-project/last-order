openapi: 3.0.0

info:
  title: Last-Order API specification
  description: The specification of Last-Order client
  version: "0.1.0-beta"
  contact:
    name: Aoi Kondo
    email: agotadmidori@gmail.com
  license:
    name: "Apache 2.0"
    url: "https://www.apache.org/licenses/LICENSE-2.0.html"

servers:
  - url: http://{host}:{port}/api/{version}
    description: API
    variables:
      version:
        enum:
          - v0
        default: v0
      host:
        default: localhost
      port:
        default: "20001"

paths:
  /runtime/sisters:
    $ref: ./paths/runtime/version/sisters.yaml
  /runtime/last-order:
    $ref: ./paths/runtime/version/last-order.yaml
  /runtime/ping:
    $ref: ./paths/runtime/ping.yaml

tags:
  - name: runtime
    description: description
